# API reference

**Status:** scaffolded

Every endpoint this service exposes. The skeleton is generated from the route
table; the substance comes from reading each handler.

Each row needs a method and path, what a caller uses it for, its parameters,
what it requires, what comes back, and how it fails.

| Method | Path | Auth | Purpose | Request | Response | Errors |
|---|---|---|---|---|---|---|
| <!-- TODO(project-standard): one row per route --> | | | | | | |
