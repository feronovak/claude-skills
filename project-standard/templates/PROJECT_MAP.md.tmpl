# Code map

**Status:** scaffolded

Where code lives, and what each area is responsible for.

<!-- TODO(project-standard): one row per top-level source directory. Every path
     named here must resolve — a map that points at a deleted directory is worse
     than no map. -->

| Path | Responsibility |
|---|---|
| `<!-- TODO(project-standard) -->` | |
