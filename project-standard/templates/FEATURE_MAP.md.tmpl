# Product map

**Status:** scaffolded

What this product does today. Present tense only — what is open belongs in
[`NEXT_STEPS.md`](NEXT_STEPS.md), what shipped belongs in the changelog.

<!-- TODO(project-standard): nothing derives this list. Routes, entry points and
     test names suggest candidates; whether a candidate is live, partial or
     abandoned is a judgement that requires reading the code. Mark each one
     `unverified` until someone has. -->

| Feature | State | Verified against |
|---|---|---|
| `<!-- TODO(project-standard) -->` | unverified | |
