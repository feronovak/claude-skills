{
  "skill_name": "next-steps",
  "notes": "This skill ships no code, so there is no unit suite. Each fixture is a plain directory, not a git clone, because the skill must work without git. Every eval names the failure mode it discriminates against — an eval that every run passes teaches nothing.",
  "evals": [
    {
      "id": 1,
      "name": "release-boundary-is-a-version",
      "prompt": "nextsteps",
      "fixture": "versioned-product",
      "expected_output": "Names the next release as a version bump from 1.4.0, proposes a MoSCoW letter for all eight items, and reports that the Musts exceed roughly 60% of the 49-hour total if they do.",
      "failure_mode": "Scoping to a calendar window ('the next two weeks') instead of a release. The package.json declares 1.4.0; a run that never mentions a version has ignored the one signal that makes MoSCoW meaningful here.",
      "expectations": [
        "Reads package.json and names the release as a bump from 1.4.0",
        "Assigns a letter to all eight items, none left blank",
        "Gives a reason on every item whose proposed letter differs from its standing marker",
        "Notices that rate-limit the login endpoint depends on session expiry and orders them accordingly",
        "Computes the guard against the in-release total — Must plus Should plus Could, excluding Won't rows — and names the rows it summed, rather than asserting a figure without arithmetic"
      ],
      "result": "pass, re-run against the corrected text — named the boundary as a bump from 1.4.0 and stated the assumed bump size rather than picking silently, lettered all eight items, caught the stated dependency, and computed the guard against the in-release total of 14 hr (Must+Should+Could), naming the rows summed. The earlier run of this eval reported a 49-hr denominator under a rule since replaced. This run also found the skill's worked examples reused this fixture's item names; the examples were renamed so a pattern-matching run cannot appear to pass."
    },
    {
      "id": 2,
      "name": "soft-cutoff-is-declared",
      "prompt": "nextsteps",
      "fixture": "unversioned-docs",
      "expected_output": "States that the repo carries no version, that 'the next release' therefore means the next block of work, and that the cut-off is soft.",
      "failure_mode": "Silently proceeding as though a hard release boundary exists, which overstates how much discipline the round carries. Inventing a version number is the same failure in a louder form.",
      "expectations": [
        "Says explicitly that no version was found",
        "Declares the cut-off soft rather than implying a firm boundary",
        "Still completes the round — a soft boundary is a caveat, not a refusal"
      ],
      "result": "pass, re-run against the corrected text — found no manifest, declared the boundary as the next block of work and said the cut-off is soft, and completed the round. The guard tripped at ~86% on the in-release denominator (1.5 of 1.75 hr) and was carried into Step 2 rather than blocking at Step 1."
    },
    {
      "id": 3,
      "name": "refuses-two-backlogs",
      "prompt": "nextsteps",
      "fixture": "two-backlogs",
      "expected_output": "Refuses to write, naming both docs/NEXT_STEPS.md and ROADMAP.md.",
      "failure_mode": "Picking docs/NEXT_STEPS.md because it is first in the search order and writing to it. That silently ratifies a second backlog, the exact condition project-standard check 26 exists to prevent.",
      "expectations": [
        "Refuses to write to either file",
        "Names both files it found",
        "Does not triage the items anyway and offer to write later — the refusal comes before the round, not after"
      ],
      "result": "pass — stopped at Step 0, named both files, ran no triage, and diff -r against the fixture was empty. The run exposed that the search order read as first-match-wins, which would have made this refusal unreachable; fixed in 43d9079 and re-run confirmed the amended wording leaves no room to guess."
    },
    {
      "id": 4,
      "name": "declines-the-uncomputable-guard",
      "prompt": "nextsteps",
      "fixture": "no-estimates",
      "expected_output": "Runs the round and states that the 60% guard cannot be checked because no item carries an effort estimate.",
      "failure_mode": "Producing a percentage anyway from invented effort figures. A fabricated number here is worse than no number, because it looks like the guard ran.",
      "expectations": [
        "Assigns a letter to all five items",
        "States that no effort estimates are present",
        "Produces no percentage and no invented hour figures"
      ],
      "result": "pass, re-run against the corrected text — lettered all five items, declined the guard explicitly, and stated no percentage, ratio or hour figure anywhere. Applied a Won't flavour using the spare-capacity test."
    },
    {
      "id": 5,
      "name": "separates-human-tasks-from-buildable",
      "prompt": "nextsteps",
      "fixture": "human-tasks",
      "expected_output": "Letters only the two buildable rows. Moves the off-site backup purchase, the key rotation, the search-vendor decision and the paid ranking tier to a Yours section, with what each needs. Reports that no buildable row sits in the top band, so the development scope has no urgent item.",
      "failure_mode": "Assigning MoSCoW letters to work the assistant cannot perform. The key rotation is the sharpest case — the row says the CLI is user-only and blocked for automation, so a run that scopes it as a Must has scoped work it is structurally forbidden from doing.",
      "expectations": [
        "Letters exactly two rows: thumbnailing and request logging",
        "Moves the backup purchase, the key rotation and the vendor decision to a Yours section, none of them lettered",
        "Gives no priority marker to any row under Yours",
        "Sends the paid ranking tier to Yours, per the tiebreaker for a trivial build behind a substantive gate, rather than scoping it",
        "Notes that the only near-term row in the file is not buildable, so the development scope has no urgent item"
      ],
      "result": "pass — lettered only the buildable rows, moved the backup purchase, the key rotation and the vendor decision to Yours with what each needs, and reported that no buildable row sits in the top band so the development scope has no urgent item. The paid ranking tier went to Yours by the trivial-build-behind-a-substantive-gate tiebreaker, cited as a rule rather than judged."
    },
    {
      "id": 6,
      "name": "writes-in-the-projects-own-scale",
      "prompt": "nextsteps",
      "fixture": "foreign-scale",
      "expected_output": "Reports the scale as P0/P1/P2 read from the legend, and writes every verdict as P0, P1 or P2. No 🟥/🟧/⬜ appears anywhere in the output or the proposed file.",
      "failure_mode": "Importing the skill's documentation examples as the vocabulary — proposing 🟥/🟧/⬜ into a project that uses P0/P1/P2. The examples in SKILL.md and moscow.md use emoji bands, so a run that copies them rather than reading this project's legend will corrupt the file.",
      "expectations": [
        "Names the scale as P0/P1/P2 and says it came from the legend line",
        "Maps Must to P0, Should to P1, Could and Won't-this-release to P2",
        "Emits no 🟥, 🟧 or ⬜ anywhere",
        "Does not propose adding a legend — the file already has one"
      ],
      "result": "pass — read P0/P1/P2 from the legend, mapped Must to P0, Should to P1, Could and Won't-this-release to P2, and emitted no emoji anywhere. Correctly stayed silent on the vocabulary question because the file already had a legend."
    },
    {
      "id": 7,
      "name": "asks-before-adopting-a-scale",
      "prompt": "nextsteps",
      "fixture": "no-scale",
      "expected_output": "Reports that the backlog has no priority scale, and asks which to adopt rather than choosing one. Proposes MoSCoW words as the safe default and offers to write a legend line.",
      "failure_mode": "Silently adopting a scale — most likely the emoji bands from the skill's own examples — into a repository that never asked for one. Introducing a vocabulary into someone else's project without asking is the failure this eval exists to catch.",
      "expectations": [
        "States plainly that no scale was found",
        "Asks before adopting one rather than picking silently",
        "Offers MoSCoW words as the default and explains they need no legend to read",
        "Does not write any emoji band into the proposal"
      ],
      "result": "run twice. The first run found the scale-adoption ask was documented in a reference and wired into no step, so the round could never reach it; that is now wired into Step 4. Re-run after the blocker fixes is pending."
    }
  ]
}
